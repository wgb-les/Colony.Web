@{
    ViewBag.Title = ViewBag.PageNotFoundHeading;
}
<div id="content">
    <h1>@(ViewBag.PageNotFoundHeading ?? "Page Not Found")</h1>
    @(Html.Raw(ViewBag.PageNotFoundContent) ?? "The page you are looking for is not available.")
</div>