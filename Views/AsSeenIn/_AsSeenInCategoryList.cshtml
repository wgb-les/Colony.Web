@model IEnumerable<WalkerGreenbank.Modules.AsSeenIn.Models.AsSeenInCategory>

<ul class="categoryList">
    @foreach (var item in Model)
    {
        Html.RenderPartial("_CategoryItem", item);
    }
</ul>